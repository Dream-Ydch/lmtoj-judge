#include <bits/stdc++.h>
#define int long long
using namespace std;

signed main() {
    ios::sync_with_stdio(false), cin.tie(nullptr);
    pair<int, int> plus;
    cin >> plus.first >> plus.second;
    cout << plus.first + plus.second << '\n';
    return 0;
}